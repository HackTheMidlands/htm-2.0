// Module Imports
// Helper imports
// Component imports
// Style imports
import React from 'react';

// Image imports

/**
 * MissionBlock components
 * @param props
 * @returns {*}
 * @constructor
 */
export const Example = (props) => {
    console.log(props);
    return <div />;
};

// Prop definitions
Example.propTypes = {};

// Default props
Example.defaultProps = {};
